<Window x:Class="GemiHotkeyProvider.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GemiHotkeyProvider.App"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="" Height="800" Width="1100"
        Loaded="MainWindow_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="8" LastChildFill="True" Visibility="Collapsed">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <Button Name="BackButton" Width="36" Margin="0,0,4,0" Content="←" Click="BackButton_OnClick" />
                <Button Name="ForwardButton" Width="36" Margin="0,0,4,0" Content="→" Click="ForwardButton_OnClick" />
                <Button Width="36" Margin="0,0,8,0" Content="⟳" Click="ReloadButton_OnClick" />
                <Button Margin="0,0,8,0" Content="Open in Browser" Click="OpenInBrowserButton_OnClick" />
            </StackPanel>

            <TextBox Name="AddressTextBox"
                     VerticalContentAlignment="Center"
                     KeyDown="AddressTextBox_OnKeyDown" />
        </DockPanel>

        <wv2:WebView2 Name="Browser" Grid.Row="1" />
    </Grid>
</Window>
